<h1>Restaurant's Tables</h1>

<ul>
  <%  @tables.each do |table| %>
    <li>
      <%= link_to table.name, table_path(table) %>
    </li>
  <% end %>
</ul>
<%= link_to "Add a Table", tables_new_path %>